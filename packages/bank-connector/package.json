{
  "name": "@sentropic/openerp-bank-connector",
  "version": "0.1.0",
  "private": true,
  "description": "OpenERP bank connector skeleton — FDX-normalized accounts/transactions exposed as an MCP server (Plaid sandbox + OFX upload providers). Custody moves to the Sentropic platform (see docs/studies/2026-07-06-sentropic-connecteur-plaid-mutualise.md).",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rhanka/openerp.git",
    "directory": "packages/bank-connector"
  },
  "type": "module",
  "main": "./dist/mcp-server.js",
  "types": "./dist/mcp-server.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.3",
    "tsx": "^4.22.0"
  }
}
