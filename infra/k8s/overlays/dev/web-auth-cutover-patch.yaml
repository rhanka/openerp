apiVersion: apps/v1
kind: Deployment
metadata:
  name: openerp-web
spec:
  template:
    spec:
      containers:
      - name: web
        env:
        - name: OPENERP_PLATFORM_AUTH_UI_ENABLED
          valueFrom:
            configMapKeyRef:
              name: openerp-auth-cutover
              key: enabled
